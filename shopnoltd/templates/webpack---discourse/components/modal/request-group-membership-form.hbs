import { precompileTemplate } from "@ember/template-compilation";
export default precompileTemplate("<DModal\n  @title={{this.title}}\n  @closeModal={{@closeModal}}\n  @inline={{@inline}}\n  class=\"request-group-membership-form\"\n>\n  <:body>\n    <div class=\"control-group\">\n      <label>\n        {{i18n \"groups.membership_request.reason\"}}\n      </label>\n\n      <ExpandingTextArea\n        {{on \"input\" (with-event-value (fn (mut this.reason)))}}\n        value={{this.reason}}\n        maxlength=\"5000\"\n      />\n    </div>\n  </:body>\n\n  <:footer>\n    <DButton\n      @action={{this.requestMember}}\n      @label=\"groups.membership_request.submit\"\n      @disabled={{this.disableSubmit}}\n      class=\"btn-primary\"\n    />\n\n    <DModalCancel @close={{@closeModal}} />\n    <ConditionalLoadingSpinner @size=\"small\" @condition={{this.loading}} />\n  </:footer>\n</DModal>",{ moduleName: "discourse/components/modal/request-group-membership-form.hbs" })