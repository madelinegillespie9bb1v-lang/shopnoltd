import {
    precompileTemplate
} from "@ember/template-compilation";
export default precompileTemplate("<p class=\"reason\">\n  <PinnedOptions @value={{this.pinned}} @topic={{this.topic}} />\n  <span class=\"text\">{{html-safe this.reasonText}}</span>\n</p>", {
    moduleName: "select-kit/components/pinned-button.hbs"
});