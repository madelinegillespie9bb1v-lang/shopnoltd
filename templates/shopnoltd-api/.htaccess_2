<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /shopnoltd-api/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Make sure that the index file is correctly specified.
DirectoryIndex index.php
