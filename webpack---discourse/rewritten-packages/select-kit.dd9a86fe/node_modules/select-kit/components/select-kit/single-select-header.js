/* import __COLOCATED_TEMPLATE__ from './single-select-header.hbs.js'; */
import {
    computed
} from "@ember/object";
import {
    or
} from "@ember/object/computed";
import {
    attributeBindings,
    classNames,
    tagName
} from "@ember-decorators/component";
import I18n from "discourse-i18n";
import SelectKitHeaderComponent from "select-kit/components/select-kit/select-kit-header";
const SingleSelectHeader = dt7948.c(class SingleSelectHeader extends SelectKitHeaderComponent {
    static {
        dt7948.g(this.prototype, "ariaLabel", [or("selectKit.options.headerAriaLabel", "name")]);
    }#
    ariaLabel = (dt7948.i(this, "ariaLabel"), void 0);
    focusIn(event) {
        event.stopImmediatePropagation();
        document.querySelectorAll(".select-kit-header").forEach(header => {
            if (header !== event.target) {
                header.parentNode.open = false;
            }
        });
    }
    get name() {
        if (this.selectedContent) {
            return I18n.t("select_kit.filter_by", {
                name: this.getName(this.selectedContent)
            });
        } else {
            return I18n.t("select_kit.select_to_filter");
        }
    }
    static {
        dt7948.n(this.prototype, "name", [computed("selectedContent.name")]);
    }
}, [attributeBindings("name", "ariaLabel:aria-label"), classNames("single-select-header"), tagName("summary")]);
export default SingleSelectHeader;